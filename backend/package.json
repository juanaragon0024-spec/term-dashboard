{
  "name": "term-web",
  "version": "1.0.0",
  "description": "Term en el navegador: sirve la interfaz y la API en un solo puerto.",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:ui && node server.js",
    "build:ui": "cd ../frontend && npm install --silent && npm run build",
    "serve": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
